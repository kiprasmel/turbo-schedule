{
	"name": "turbo-schedule",
	"private": true,
	"workspaces": {
		"packages": [
			"common",
			"scraper",
			"server",
			"client"
		]
	},
	"scripts": {
		"purge": "rimraf ./*/node_modules && rimraf ./node_modules",
		"reinstall": "yarn purge && yarn install",
		"dev": "yarn --cwd server/ run dev",
		"test": "lerna run test",
		"build": "lerna run build",
		"cleanbuild": "yarn reinstall && yarn build",
		"serve": "yarn --cwd server run serve",
		"full-deploy": ""
	},
	"devDependencies": {
		"lerna": "^3.16.4",
		"rimraf": "^3.0.0"
	}
}
